apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: auth-ingress
  labels:
  annotations:
    kubernetes.io/ingress.class: 'kong'
spec:
  rules:
    - host: {{ .Values.auth.host }}
      http:
        paths:
          - path: /*
            pathType: Prefix
            backend:
              service:
                name: auth-svc
                port:
                  number: 80
